#ifndef SUM_LIB_H
#define SUM_LIB_H

#include <stddef.h>

// Вычисляет сумму элементов массива от start до end (не включая end)
long long calculate_sum(const int *array, size_t start, size_t end);

#endif
